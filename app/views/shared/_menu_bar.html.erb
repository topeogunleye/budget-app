  <nav class="navbar navbar-expand-lg navbar-dark primary d-flex justify-content-between">
    <%= link_to "Transactions Manager", root_path, class: "nav-link" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <% if user_signed_in? %>
          <li class="nav-item <%= 'active' if current_page?(categories_path) %>">
            <%= link_to "Categories", categories_path, class: "nav-link" %>
          </li>
          <li class="nav-item <%= 'active' if current_page?(categories_new_path) %>">
            <%= link_to "New Category", categories_new_path, class: "nav-link" %>
          </li>
          <li class="nav-item <%= 'active' if current_page?(destroy_user_session_path) %>">
            <%= button_to "Log Out", destroy_user_session_path, method: :delete, class: "btn btn-danger" %>
          </li>
        <% else %>
          <li class="nav-item <%= 'active' if current_page?(new_user_registration_path) %>">
            <%= link_to "Sign Up", new_user_registration_path, class: "nav-link" %>
          </li>
          <li class="nav-item <%= 'active' if current_page?(new_user_session_path) %>">
            <%= link_to "Log In", new_user_session_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </nav>
